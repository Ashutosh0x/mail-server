<?xml version="1.0" encoding="utf-8"?>
<!--
  Nothing leaves the device through backup. The cached mail database, the
  encrypted session store and the Keystore material are all excluded: a backup
  is an unencrypted copy of a mailbox sitting in someone's cloud account.
-->
<data-extraction-rules>
    <cloud-backup>
        <exclude domain="root" />
        <exclude domain="database" />
        <exclude domain="sharedpref" />
        <exclude domain="file" />
    </cloud-backup>
    <device-transfer>
        <exclude domain="root" />
        <exclude domain="database" />
        <exclude domain="sharedpref" />
        <exclude domain="file" />
    </device-transfer>
</data-extraction-rules>
